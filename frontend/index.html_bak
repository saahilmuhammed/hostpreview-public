<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>NFPreview - Preview Your Website</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex h-100 text-center">
  <div class="container">
    <h1>Preview Your Website</h1>
    <p>Instantly preview your website changes before going live. Test updates, troubleshoot issues, and ensure a seamless experience for your visitors without modifying your DNS or host files.</p>
    <form id="tempUrlForm">
      <div class="mb-3">
        <input type="text" class="form-control" id="domain" name="domain" placeholder="domain.com" required>
      </div>
      <div class="mb-3">
        <input type="text" class="form-control" id="ip" name="ip" placeholder="Server IP" required>
      </div>
      <button type="submit" class="btn btn-primary">Create Preview Link</button>
    </form>
    <div id="loading" style="display:none;">Generating Preview Link... Please wait.</div>
    <div id="output"></div>
    <p>Website preview link expires after <mark>3</mark> days</p>
  </div>
  <script>
    document.getElementById('tempUrlForm').addEventListener('submit', async e => {
      e.preventDefault();
      document.getElementById('loading').style.display = 'block';
      const domain = e.target.domain.value;
      const ip = e.target.ip.value;
      const response = await fetch('/api/preview-link', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({domain, ip})
      });
      const data = await response.json();
      document.getElementById('loading').style.display = 'none';
      document.getElementById('output').innerHTML = `<a href="${data.previewUrl}" target="_blank">${data.previewUrl}</a>`;
    });
  </script>
</body>
</html>

